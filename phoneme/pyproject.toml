[project]
name = "phoneme"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pronouncing>=0.2.0",
    "pydantic>=2.10.6",
    "setuptools>=75.8.0",
]

[dependency-groups]
dev = [
    "pytest>=8.3.4",
]
